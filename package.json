{
  "name": "course-scheduler",
  "version": "0.1.3",
  "description": "Generate course schedules",
  "main": "dist/index.js",
  "dependencies": {
    "@types/chai": "^3.5.0",
    "@types/mocha": "^2.2.40",
    "@types/node": "^7.0.13",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "rollup": "^0.41.6",
    "ts-node": "^3.0.2",
    "tslint": "^5.1.0",
    "typescript": "^2.2.2"
  },
  "devDependencies": {},
  "scripts": {
    "install": "npm run build",
    "build": "tsc && rollup -c",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "lint": "tslint --project tsconfig.json --type-check src/*.ts test/*.ts",
    "pretest": "tsc test/*.ts",
    "test": "mocha",
    "clean": "rm -f dist/* src/*.js dist/*.js"
  },
  "author": "Rizadh Nizam",
  "repository": {
    "type": "git",
    "url": "https://github.com/rizadh/course-scheduler.git"
  },
  "license": "Unlicense"
}
